import "tfrun"
import "decimal"

param monthly_cost default 100

delta_monthly_cost = decimal.new(tfrun.cost_estimate.delta_monthly_cost)

main = rule {
    delta_monthly_cost.less_than(monthly_cost)
}

